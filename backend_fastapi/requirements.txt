# Disaster Resilience AI — Backend Dependencies
# Install with: pip install -r requirements.txt

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
pydantic[email]>=2.6.0

# Database
supabase>=2.0.0
python-dotenv>=1.0.0

# HTTP client for external APIs (MetMalaysia, Open-Meteo)
httpx>=0.27.0

# Background job scheduler
apscheduler>=3.10.0

# Phone number validation
phonenumbers>=8.13.0

# JWT authentication
PyJWT>=2.8.0

# Twilio SMS (mock-safe — only used if credentials supplied)
twilio>=8.10.0

# Form data parsing (required for SMS webhook endpoint)
python-multipart>=0.0.9
